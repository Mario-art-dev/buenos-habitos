name: Servidor temporal

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: escenas-server
  cancel-in-progress: false

jobs:
  server:
    if: ${{ vars.SERVER_ENABLED != 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Descargar el código
        uses: actions/checkout@v4

      - name: Restaurar la galería desde el respaldo permanente (rama git "gallery-data")
        # POR QUÉ YA NO SE USA "actions/cache" PARA ESTO (aunque sea más rápido): confirmado con
        # logs reales, no adivinado, que la caché de GitHub Actions NUNCA fue almacenamiento
        # duradero de verdad, por mucho que se separara la base de datos de los vídeos (fix
        # anterior, insuficiente): tiene un tope de 10GB por repositorio con desalojo automático
        # (un solo checkpoint de ~12GB ya lo supera él solo y se acaba desalojando ENTERO sin
        # avisar) y además cada entrada caduca sola a los 7 días sin usarse, pase lo que pase con
        # el tamaño. Así se perdieron los ~40 shorts de la sesión temporal 89: cuando se
        # comprobó, la caché ya había desaparecido por completo ("Cache not found for input
        # keys" total) — y no hay forma de deshacer eso, la caché no guarda versiones.
        #
        # El arreglo de verdad: en vez de una caché de build (pensada para ser desechable),
        # storage/ (la base de datos Y los vídeos) se respalda en una rama de git normal
        # ("gallery-data") de este mismo repositorio, con un commit real por checkpoint. Una
        # rama de git NO caduca nunca y NO se desaloja por peso — es simplemente parte del
        # historial del repositorio hasta que alguien la borre a mano. Es el único mecanismo
        # gratuito (cero tarjeta, cero cuenta de pago) que da persistencia de verdad aquí.
        run: |
          set -e
          git config --global user.email "actions@github.com"
          git config --global user.name "Escenas Virales Bot"
          mkdir -p storage
          REMOTE_URL="https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          rm -rf /tmp/gallery-backup
          if timeout 120 git clone --branch gallery-data --single-branch --depth 1 "$REMOTE_URL" /tmp/gallery-backup 2>/tmp/gallery-clone.log; then
            echo "Rama de respaldo permanente encontrada, restaurando..."
            if [ -d /tmp/gallery-backup/storage ]; then
              cp -a /tmp/gallery-backup/storage/. storage/
              echo "Restaurado en storage/: $(du -sh storage 2>/dev/null | cut -f1)"
            else
              echo "La rama de respaldo existe pero todavía no tiene ninguna carpeta storage/ guardada."
            fi
          else
            echo "Todavía no existe la rama de respaldo 'gallery-data' (primera vez que arranca este mecanismo, o el historial anterior a este cambio se perdió por el bug de la caché ya descrito arriba)."
            cat /tmp/gallery-clone.log || true
            mkdir -p /tmp/gallery-backup
            cd /tmp/gallery-backup
            git init -q -b gallery-data
            git remote add origin "$REMOTE_URL"
          fi

      - name: Diagnóstico — comprobar qué hay realmente en storage/ tras restaurar
        # Deja constancia en el log de qué hay EXACTAMENTE en storage/ justo después de restaurar
        # (paso de arriba), para poder confirmar de un vistazo si esta vez sí ha llegado algo.
        run: |
          echo "--- contenido de storage/ ---"
          ls -la storage/ 2>&1 || echo "(storage/ no existe)"
          echo "--- buscando app.db en todo el árbol ---"
          find storage -maxdepth 3 -iname "app.db*" -exec ls -la {} \; 2>&1 || true
          echo "--- espacio en disco ---"
          df -h . || true

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Instalar ffmpeg, yt-dlp y dependencias de Python
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3 python3-pip fonts-dejavu-core fonts-liberation fonts-comic-neue
          pip3 install --no-cache-dir --break-system-packages yt-dlp faster-whisper piper-tts librosa bgutil-ytdlp-pot-provider yt-dlp-invidious

      - name: Instalar fuentes libres para el editor de texto del clip
        run: |
          sudo mkdir -p /usr/share/fonts/truetype/custom
          # Google Fonts (licencia OFL/Apache), directo del repo oficial: no todas están
          # empaquetadas para apt. "%5B"/"%5D" son "[" "]" codificados porque curl interpreta
          # corchetes sin codificar como un rango/glob, no como parte literal de la URL.
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/BebasNeue-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/bebasneue/BebasNeue-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Anton-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/anton/Anton-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Montserrat-Variable.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/montserrat/Montserrat%5Bwght%5D.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Poppins-Bold.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/poppins/Poppins-Bold.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Oswald-Variable.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/oswald/Oswald%5Bwght%5D.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/PermanentMarker-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/apache/permanentmarker/PermanentMarker-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Bangers-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/bangers/Bangers-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Lobster-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/lobster/Lobster-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/ArchivoBlack-Regular.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/archivoblack/ArchivoBlack-Regular.ttf"
          sudo curl -fsSL -o /usr/share/fonts/truetype/custom/Caveat-Variable.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/caveat/Caveat%5Bwght%5D.ttf"
          sudo fc-cache -f

      - name: Instalar el modelo de IA local de repuesto (Ollama) — nunca bloquea el arranque
        # Último eslabón de la cadena de proveedores de IA (ver src/lib/ai/ollama.ts): un modelo
        # abierto corriendo en este mismo servidor, sin clave, sin cuenta, sin tarjeta, sin cupo
        # que se pueda agotar — solo se usa si todos los proveedores en la nube fallan a la vez.
        # continue-on-error de verdad: si algo aquí falla (red bloqueada, instalador caído, lo que
        # sea), la sesión sigue exactamente igual que sin este paso — nunca puede romper el
        # arranque de la web/worker. La descarga del modelo se lanza en segundo plano (no se
        # espera aquí) para no restar tiempo a los pasos siguientes; con el tiempo que tardan
        # (fuentes, npm ci, imagen de Docker, compilar) suele terminar de sobra antes de que haga
        # falta de verdad.
        continue-on-error: true
        run: |
          set +e
          curl -fsSL https://ollama.com/install.sh | sh
          nohup ollama serve > ollama.log 2>&1 &
          for i in $(seq 1 30); do
            curl -s -o /dev/null http://127.0.0.1:11434 && break
            sleep 1
          done
          nohup sh -c 'ollama pull llama3.2:3b; ollama pull moondream' > ollama-pull.log 2>&1 &
          exit 0

      - name: Instalar dependencias de Node
        run: npm ci

      - name: Iniciar el proveedor de tokens de YouTube (evita el bloqueo por IP de datacenter)
        run: |
          docker pull brainicism/bgutil-ytdlp-pot-provider
          docker run -d --init -p 4416:4416 --name bgutil-provider brainicism/bgutil-ytdlp-pot-provider
          ready=""
          for i in $(seq 1 30); do
            if curl -s -o /dev/null http://127.0.0.1:4416; then
              ready=1
              echo "Proveedor de tokens listo."
              break
            fi
            sleep 2
          done
          if [ -z "$ready" ]; then
            echo "El proveedor de tokens no respondió a tiempo. Logs del contenedor:"
            docker logs bgutil-provider || true
            exit 1
          fi

      - name: Preparar la base de datos (reutilizando la del respaldo si existe)
        run: npx prisma generate && npx prisma db push
        env:
          DATABASE_URL: "file:./storage/app.db"

      - name: Compilar la web
        run: npm run build
        env:
          DATABASE_URL: "file:./storage/app.db"
          APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}

      - name: Arrancar la web y el worker en segundo plano
        run: |
          nohup npm run start > web.log 2>&1 &
          nohup npx tsx worker/index.ts > worker.log 2>&1 &
        env:
          DATABASE_URL: "file:./storage/app.db"
          APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          AI_PROVIDER: "groq"
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # Cadena de repuesto: si se ponen estos dos secretos (ambos con capa gratuita, ver
          # src/lib/ai/cerebras.ts y mistral.ts), se usan solos cuando Groq/Gemini se queden sin
          # cupo — no hace falta tocar nada más aquí, el orden lo decide FALLBACK_ORDER en
          # src/lib/ai/provider.ts.
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
          # Último eslabón de repuesto: modelo local sin clave (ver el paso de arriba que lo
          # instala) — si por lo que sea no llegó a instalarse/descargarse a tiempo, una llamada
          # aquí simplemente falla y la cadena de proveedores sigue funcionando igual que sin él.
          OLLAMA_ENABLED: "true"
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          # Sin estos cuatro, config.google/config.tiktok se quedan vacíos y la web no puede
          # conectar YouTube/TikTok aunque el código de OAuth esté bien — hacían falta como
          # secretos de GitHub PERO nunca se pasaban aquí al proceso.
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          TIKTOK_CLIENT_KEY: ${{ secrets.TIKTOK_CLIENT_KEY }}
          TIKTOK_CLIENT_SECRET: ${{ secrets.TIKTOK_CLIENT_SECRET }}
          TRANSCRIPTION_PROVIDER: "local"
          PYTHON_PATH: "python3"
          TTS_PROVIDER: "local"
          ENABLE_COMMENTARY: "false"
          ENABLE_DYNAMIC_ZOOM: "true"
          ENABLE_HOOK_FRAME_CHECK: "true"
          ENABLE_BIG_CAPTIONS: "true"
          ENABLE_COVER_CARD: "true"
          CHANNEL_NAME: "Escenas Virales"
          CHANNEL_NICHE: "Recopilación de los momentos más divertidos, graciosos y de entretenimiento de creadores de contenido — humor, giros inesperados y reacciones, pensado para el público de comedia/entretenimiento de shorts"
          # Sin fijar CHANNEL_LANGUAGE aquí: se usa el valor por defecto de la app (inglés,
          # más ingresos publicitarios). Antes esto estaba fijado a "es" y anulaba en silencio
          # ese valor por defecto en todo momento.
          # La programación automática (franjas horarias) necesita saber la zona horaria real
          # para que "12:00-14:00" signifique la hora de España y no UTC.
          TZ: "Europe/Madrid"

      - name: Esperar a que la web responda
        run: |
          for i in $(seq 1 30); do
            if curl -s -o /dev/null http://localhost:3000; then
              echo "Web lista."
              exit 0
            fi
            sleep 2
          done
          echo "La web no arrancó a tiempo, revisa web.log"
          cat web.log
          exit 1

      - name: Abrir el túnel público (Cloudflare, sin cuenta)
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          nohup ./cloudflared tunnel --url http://localhost:3000 > cloudflared.log 2>&1 &

          URL=""
          for i in $(seq 1 20); do
            URL=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared.log | head -1 || true)
            if [ -n "$URL" ]; then break; fi
            sleep 2
          done

          if [ -z "$URL" ]; then
            echo "No se pudo abrir el túnel."
            cat cloudflared.log
            exit 1
          fi

          echo "## 🔗 Tu panel está listo" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "**$URL**" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Entra con la contraseña que pusiste en el secreto \`APP_PASSWORD\`." >> "$GITHUB_STEP_SUMMARY"
          echo "Esta URL cambiará dentro de ~5h, cuando esta sesión pase el testigo a la siguiente." >> "$GITHUB_STEP_SUMMARY"
          echo "[cloudflared] URL pública: $URL"

      # A partir de aquí: el ÚNICO guardado real de la galería. Cada 10 minutos se hace un commit
      # de verdad con el contenido actual de storage/ (base de datos + vídeos) a la rama de git
      # "gallery-data" — nunca caduca, nunca se desaloja por peso, sobrevive a que esta sesión
      # temporal termine, se cancele o se caiga por cualquier motivo (como mucho se pierden los
      # últimos 10 minutos de shorts generados, no la sesión entera). Ya NO se usa
      # "actions/cache" para esto — motivo completo en el primer paso de este archivo.
      #
      # BUG REAL CONFIRMADO Y CORREGIDO (sesión temporal 97, con logs reales): el job entero tiene
      # un tope DURO de 360 minutos (timeout-minutes de este workflow) que GitHub Actions mata sin
      # avisar si se supera — y si eso pasa ANTES de llegar al último paso ("Pasar el testigo"),
      # nadie relanza la siguiente sesión: el servidor se queda caído del todo hasta que alguien
      # lo note y lo arranque a mano. Con 33 checkpoints de 10 min (330 min) más el arranque
      # (~4 min) solo quedaban ~26 min de margen — y una sola operación de red (git push/fetch)
      # que se quede colgada sin límite propio basta para comerse ese margen y hacer que el job
      # entero muera a mitad de un checkpoint, justo lo que pasó. Arreglo doble: (1) bajado a 30
      # checkpoints (300 min) para dejar ~55 min de margen real antes del tope de 360, y (2) cada
      # operación de red de git en los checkpoints lleva ahora su propio "timeout" (nunca puede
      # colgarse sin límite, como mucho falla rápido y el reintento con backoff ya existente se
      # encarga del resto).


      - name: Mantener la sesión activa (checkpoint 1/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 1/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 1/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 2/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 2/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 2/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 3/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 3/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 3/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 4/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 4/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 4/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 5/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 5/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 5/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 6/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 6/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 6/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 7/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 7/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 7/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 8/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 8/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 8/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 9/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 9/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 9/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 10/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 10/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 10/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 11/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 11/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 11/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 12/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 12/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 12/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 13/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 13/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 13/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 14/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 14/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 14/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 15/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 15/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 15/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 16/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 16/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 16/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 17/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 17/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 17/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 18/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 18/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 18/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 19/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 19/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 19/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 20/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 20/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 20/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 21/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 21/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 21/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 22/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 22/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 22/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 23/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 23/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 23/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 24/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 24/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 24/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 25/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 25/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 25/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 26/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 26/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 26/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 27/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 27/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 27/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 28/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 28/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 28/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 29/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 29/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 29/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi

      - name: Mantener la sesión activa (checkpoint 30/30 de la galería)
        run: sleep 600

      - name: Guardar el respaldo permanente (checkpoint 30/30) — rama git, nunca caduca ni se desaloja
        run: |
          cd /tmp/gallery-backup
          # Copia completa en vez de una sincronización incremental: no depende de rsync (no
          # viene garantizado en todos los runners) y así los archivos borrados en storage/ (p.ej.
          # limpieza futura de vídeos ya publicados) tampoco se quedan para siempre en el respaldo.
          rm -rf storage
          mkdir -p storage
          cp -a "$GITHUB_WORKSPACE/storage/." storage/
          # git rechaza de raíz cualquier archivo de más de 100MB: lo excluimos para que un short
          # pesado no rompa el guardado de TODOS los demás.
          find storage -type f -size +95M -print | while read -r f; do
            echo "Aviso: '$f' pesa más de 95MB, se excluye de este checkpoint del respaldo permanente."
            rm -f "$f"
          done
          git add -A
          committed=""
          if git rev-parse --verify HEAD >/dev/null 2>&1 && git diff --cached --quiet; then
            echo "Sin cambios nuevos que respaldar."
          elif git commit -q -m "Snapshot automático checkpoint 30/30 ($(date -u +%FT%TZ))"; then
            committed=1
          else
            echo "Nada que respaldar todavía (storage/ vacío)."
          fi
          if [ -n "$committed" ]; then
            ok=""
            for attempt in 1 2 3 4; do
              if timeout 90 git push -q origin HEAD:gallery-data; then
                ok=1
                break
              fi
              echo "Fallo al subir el respaldo (intento $attempt/4), reintentando..."
              timeout 60 git fetch -q origin gallery-data 2>/dev/null || true
              git merge -q -X ours --no-edit origin/gallery-data 2>/dev/null || true
              sleep $((attempt * 5))
            done
            if [ -z "$ok" ]; then
              echo "AVISO: no se pudo subir el respaldo tras 4 intentos. Se reintentará en el siguiente checkpoint (como mucho se pierden 10 minutos)."
            fi
          fi
      - name: Pasar el testigo a la siguiente sesión
        if: ${{ vars.SERVER_ENABLED != 'false' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run server.yml --ref ${{ github.ref_name }}
